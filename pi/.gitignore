# Pi agent config — never commit machine-local / secret files.
# (These shouldn't be placed here, but this is a safety net.)

# Credentials
auth.json

# Machine-local state
sessions/
trust.json
models-store.json

# Package installs
git/
npm/

# Node deps (from pi-skills, if installed here)
node_modules/
